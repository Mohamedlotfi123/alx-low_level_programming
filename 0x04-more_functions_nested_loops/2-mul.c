#include "main.h"
/**
 * mul - multiplies two integers.
 * @a : first number will be multiply
 * @b : second number will be multiply
 *
 * Return: result of multiplication
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
